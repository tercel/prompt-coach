---
description: prompt-coach — show current state (each feature, scope, state-file path)
---

Run EXACTLY this Bash command and show its stdout verbatim — do nothing else:

```bash
python3 "${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}/scripts/coach.py" --ctl status
```
